const navLinks=document.querySelectorAll('.nav a[href^="#"]');navLinks.forEach(a=>a.addEventListener('click',e=>{e.preventDefault();document.querySelector(a.getAttribute('href')).scrollIntoView({behavior:'smooth'})}));
const tabButtons=document.querySelectorAll('.tab');const panes=document.querySelectorAll('.tabpane');tabButtons.forEach(btn=>btn.addEventListener('click',()=>{tabButtons.forEach(b=>b.classList.remove('is-active'));panes.forEach(p=>p.classList.remove('is-active'));btn.classList.add('is-active');document.getElementById(btn.dataset.tab).classList.add('is-active')}));
const rates={THB:1,EUR:0.025,USD:0.028};const priceEl=document.getElementById('price-value');const curEl=document.getElementById('price-currency');document.querySelectorAll('.chip').forEach(chip=>chip.addEventListener('click',()=>{const cur=chip.dataset.cur;const thb=Number(priceEl.dataset.thb);const value=Math.round(thb*rates[cur]).toLocaleString();priceEl.textContent=value;curEl.textContent=cur;}));document.getElementById('year').textContent=new Date().getFullYear();
